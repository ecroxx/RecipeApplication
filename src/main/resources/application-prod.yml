spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipeapp_prod
    username: prod_user
    password: password_prod
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false